# 会话推进协议（NextStep）v2.1

> 方案构建类本体主文档。目的：用户**零记忆**调用某仓库/团队沉淀的全部能力——选项递到眼前，
> 回一个数字就能推进；想干菜单外的事永远不受限（混合制：结构化选项做主干，自由输入做逃生门）。
> **本协议是通用的**：不绑定任何团队、项目或工具；v1.x 中的实验室菜单自 v2.0 起降级为「部署实例」，不随本协议分发。

## 一、适用范围与承载

- 适用：任何支持 **AGENTS.md 开放标准** 的 AI 编码助手会话（ZCode / Cursor / Codex 等均自动读取
  仓库根 `AGENTS.md`；Claude Code 用户可将同内容放 `CLAUDE.md`）。
- **安装即全对话生效、跨会话持久**——不是 skill（需触发）、不是单次对话约束（临时注入），
  而是 AI 每次会话自动读取的仓库级常驻配置。
- 承载文件：仓库根 `AGENTS.md`；内容 = `skills/nextstep/assets/` 通用模板 + 你自己登记的能力行。

## 二、分层模型（通用性的关键）

| 层 | 内容 | 是否随产品分发 |
|----|------|---------------|
| 协议层 | 本文件的机制与纪律（三机制/防噪/兜底/安装规则）——**对所有人一致** | ✅ 通用 |
| 登记层 | 能力菜单的具体行（"你说什么 → 我做什么"）——**每个仓库/团队各自登记** | ❌ 实例自建 |

读协议 = 学会一套推进方式；登记 = 把你自己沉淀的流程/脚本/规范挂上去。
登记层可承载任意多项异构流程（研发此协议时的实验室实例曾同时挂 14 项）——
实例不随协议分发，不构成协议的一部分。

## 三、三机制

### 机制① 回合脚手架（状态栏 + 编号选项）

在一轮工作**收口 / 阶段闸口 / 出现分叉**时，AI 回复末尾固定附（原生 Markdown 渲染，不用代码块）：

**在办** <当前正在推进的事> ｜ **在途** <等结果/等回执的事> ｜ **下一闸口** <最近要过的关>

**下一步（回数字即可）：**

1. <最可能的事>
2. <次可能的事>
3. 其他 —— 直接说事，不受限

规则：
- 选项从**真实上下文**取材（任务清单/待办/未决事项），不编造；
- 选项产生真实后果——回数字即执行，做完再收口再出新选项（回合循环，累积效应）；
- 状态栏即存档：在办/在途/下一闸口三件事锚定语境，不靠翻历史。

### 机制② 能力编号（编号跳转）

- 登记层中每个能力各配一个**编号**；用户任何时候说「菜单」→ AI 展示全表；
- 可直接说编号（如「B1」）调用对应能力，无需记忆触发词原文；
- **登记制度**：编号表是该仓库「用户说什么 → AI 做什么」的**唯一登记处**——新能力/新脚本/新规范
  出生时登记一行；编号一经分配不复用。
- **登记表外置**：登记超过 8 行时移入登记文件（如 `docs/capabilities.md`），`AGENTS.md` 只留分组行与指针
  ——行数红线不为登记层让步。

### 机制③ 防噪三纪律（回应"清单臃肿降智"实证）

研究实证：注入清单臃肿会降低模型表现（精确对照实验待补，见 docs/evidence.md；方向性支持见长上下文退化研究）。故：

1. **执行中途不出菜单**——只在收口/闸口/分叉处出；
2. **薄**——状态栏 1 行 + 选项 ≤5；`AGENTS.md` 全文 ≤40 行，只做导航，详细规则指向各自正文；
   输出侧同样设配额：状态栏与上轮一致则省略，同一选项不跨回合重复，宿主有原生任务/计划工具时优先用之、不在散文里复述；
3. **静默模式**——用户说「安静」= 长任务期间只干活不递菜单，下次收口自动恢复。

## 四、模糊兜底（混合制的逃生门）

- 用户请求含糊、或疑似指向已登记能力时：**不给泛答**，从编号表报出相近 2–4 项（各一句话）供选；
- 菜单外的事**永远合法**：直接说正事，AI 直接干。

## 五、编号体系

- 分组前缀按用途自定（实验室实例用 A 治理 / B 记忆 / C 产品 / D 写作；你可按你的域命名）；
- 分组对用户**可见**（编号即地址，沟通引用无歧义）；
- 表内每行四要素：编号 ｜ 你说（触发词） ｜ 我做（做什么） ｜ 详情（规则/脚本指针）。

## 六、状态栏数据源

| 字段 | 取材 | 刷新时机 |
|------|------|---------|
| 在办 | 当前会话正在推进的事 | 每次收口 |
| 在途 | 已交出去、等结果的事项 | 每次收口 |
| 下一闸口 | 时间上最近的必经环节（评审/发布/确认） | 每次收口 |

三字段写不下就省略——**宁可缺，不可编**。

## 七、安装与合并

1. **新仓库**：把 `skills/nextstep/assets/AGENTS.md`（英文会话用 `AGENTS.en.md`）复制到仓库根 `AGENTS.md`，重开会话即生效；
2. **已有 AGENTS.md 的仓库**：不覆盖——把模板中「能力菜单」与「硬规则」两节**追加合并**进现有文件
   （AGENTS.md 本就是自由文本，无格式冲突）；
3. **CLAUDE.md 用户**：同内容复制一份到 `CLAUDE.md` 即可；
4. 验证：新开会话说「菜单」，AI 回出能力表 = 安装成功。

## 八、维护与升级

- 日常登记新能力行 = 日常维护，随改随生效；
- 协议机制本身的修订（新增机制/纪律变更）= 本文件升版本；
- 编号表结构性调整（分组重排/编号变更）属正式升级，建议在登记处头部留一行变更记录。

## 九、持久性与红线（v2.1 增补）

- **持久性**：三机制适用于会话余下每一轮，直到用户明说停止；不因话题切换或轮次增加而失效。
  若不确定是否仍然适用，答案是适用。此条防的是行为指令随轮次衰减——常驻行为协议的常见失效模式，
  同类协议均显式设防，本协议自 v2.1 起补齐。
- **红线（这样不算收口）**：只报完成、没有状态栏；选项是上下文里没有的事；菜单插在执行中途；
  用代码块包裹菜单。

原第九节「实测裁决项」四条移至 `docs/open-questions.md`，随实测进展回填。
