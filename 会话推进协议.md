# 会话推进协议（NextStep）1.0

> 一份给 AI 编码助手用的推进会话方式。目的：把某个仓库或团队沉淀下来的能力递到用户眼前，
> 回一个数字就能往下走；同时不挡着用户直接说事（结构化选项做主干，自由输入当后门）。
> 协议本身是通用的，不绑定任何团队、项目或工具。早期草案里的实验室菜单已降级为部署实例，不随本协议分发。

## 一、适用范围与承载

- 适用：任何支持 AGENTS.md 开放标准的 AI 编码助手会话（ZCode / Cursor / Codex 等会自动读取
  仓库根 `AGENTS.md`；Claude Code 用户可以放 `CLAUDE.md`）。
- 装完就对整个会话生效，跨会话持续。它不是 skill（需要触发），也不是单次对话的临时约束，
  而是 AI 每次会话都会自动读到的仓库级常驻配置。
- 承载文件：仓库根 `AGENTS.md`。内容 = `templates/` 里的通用模板 + 你自己登记的能力行。

## 二、分层模型（通用性的关键）

| 层 | 内容 | 是否随产品分发 |
|----|------|---------------|
| 协议层 | 本文件的机制与纪律（三机制、防噪、兜底、安装规则），对所有人一致 | 通用 |
| 登记层 | 能力表的具体行（"你说什么 → 我做什么"），每个仓库或团队各自登记 | 实例自建 |

读协议是学会一套推进方式，登记是把你自己沉淀的流程、脚本、规范挂上去。
登记层能容纳任意多项互不相干的流程。研发这套协议时的实验室实例同时挂过 14 项。
实例不随协议分发，也不构成协议的一部分。

## 三、三机制

### 机制① 回合脚手架（状态栏 + 编号选项）

在一轮工作收口、遇到阶段闸口、或出现分叉时，AI 回复末尾固定附上（原生 Markdown 渲染，不用代码块）：

**在办** <当前正在推进的事> ｜ **在途** <等结果/等回执的事> ｜ **下一闸口** <最近要过的关>

**下一步（回数字即可）：**

1. <最可能的事>
2. <次可能的事>
3. 其他，直接说事，不受限

规则：

- 选项从真实上下文取材（任务清单、待办、未决事项），不编造；
- 选项产生真实后果：回数字即执行，做完再收口再出新选项，如此循环累积；
- 两种呈现形态语义等同：分行编号列表（规范形态，见上方骨架）和单行紧凑形式（模板导航等空间受限处用）。
  无论哪种形态都有硬性格式要求：字段标签一律加粗，字段间用 ｜ 分隔，状态栏与「下一步」之间必须空行分隔——
  否则渲染器按软换行规则把整块并成一段，结构全部糊掉；
- 状态栏本身就是存档。在办、在途、下一闸口三件事锚定语境，不靠翻历史。

### 机制② 能力编号（编号跳转）

- 登记层里每个能力各配一个编号。用户任何时候说「菜单」，AI 展示全表；
- 可以直接说编号（如「B1」）调用对应能力，不必记触发词原文；
- 登记制度：编号表是该仓库「用户说什么 → AI 做什么」的唯一登记处。新能力、新脚本、新规范
  出生时就登记一行；编号一经分配不再复用。
- 登记表外置：登记行数会让 `AGENTS.md` 越过 40 行红线时，把它移进登记文件（如 `docs/capabilities.md`，
  格式用同款四列表格，分组行占整行），`AGENTS.md` 原地只留分组行与指针，例如
  `| D 组 | 写作类 | 各写作流程登记于此 | docs/capabilities.md |`。红线自指为界，不设魔法数。
  出厂基线是中文 36 行、英文 35 行，登记容量分别是 5 行和 6 行。
  真实的大登记表（如研发期那个 14 项的实例）必然走外置流程，这是预期，不是缺陷。
  另外要留意预算竞争：模板每多一行散文，登记容量就少一行，两者共用同一个 40 行预算（容量 = 40 − 基线 + 1）。

### 机制③ 防噪三纪律（回应清单臃肿的问题）

长上下文研究和日常使用经验指向同一件事：注入越长，遵循越差。精确的对照实验还没做
（方向性支持可参考 Liu et al., 2023, *Lost in the Middle*）。所以：

1. 执行中途不出菜单，只在收口、闸口、分叉处出；
2. 薄。状态栏 1 行，选项不超过 5 个，`AGENTS.md` 全文不超过 40 行，只做导航，详细规则放到各自正文里。
   输出侧同样设配额：状态栏与上一轮一致就省略，同一选项不跨回合重复；宿主有原生任务或计划工具时，
   清单可以交给它承载，但收口脚手架照常输出，不在散文里复述工具已经承载的内容；
3. 静默模式。用户说「安静」，长任务期间就只干活不递菜单，下次收口自动恢复。

## 四、模糊兜底（自由输入的入口）

- 用户请求含糊、或疑似指向某个已登记能力时，不给泛泛的回答，从编号表里报出相近的 2 到 4 项，
  每项一句话，供用户挑；
- 菜单外的事永远合法。用户直接说正事，AI 就直接干。

## 五、编号体系

- 分组前缀按用途自定。研发期的实验室实例用 A 治理 / B 记忆 / C 产品 / D 写作，你可以按自己的域命名；
- 分组对用户可见，编号即地址，沟通引用不会歧义；
- 表里每行四个要素：编号 ｜ 你说（触发词） ｜ 我做（做什么） ｜ 详情（规则或脚本指针）。

## 六、状态栏数据源

| 字段 | 取材 | 刷新时机 |
|------|------|---------|
| 在办 | 当前会话正在推进的事 | 每次收口 |
| 在途 | 已交出去、等结果的事项 | 每次收口 |
| 下一闸口 | 时间上最近的必经环节（评审、发布、确认） | 每次收口 |

三个字段写不下就省略，宁可缺，不可编。

## 七、安装与合并

1. 新仓库：把 `templates/AGENTS.md`（英文会话用 `AGENTS.en.md`）复制到仓库根，命名 `AGENTS.md`，
   重开会话即生效；
2. 已有 `AGENTS.md` 的仓库：不要覆盖。把模板里的「能力菜单」和「硬规则」两节追加合并进现有文件。
   `AGENTS.md` 本来就是自由文本，没有格式冲突；
3. Claude Code 用户：同样内容复制一份到 `CLAUDE.md`；
4. 验证：新开会话说「菜单」，AI 回出能力表，就是装好了。

## 八、维护与升级

- 日常登记新能力行，就是日常维护，随改随生效；
- 协议机制本身的修订（新增机制、纪律变更）才升版本；
- 编号表的结构性调整（分组重排、编号变更）属于正式升级，建议在登记处头部留一行变更记录；
- 迁移：模板头部标注协议版本（当前 1.0）。升级已装的模板，就是用新模板覆盖对应几节，合并原则不变。
  版本行是判断新旧安装的唯一依据。

## 九、持久性与红线

- 持久性：三机制适用于会话余下每一轮，直到用户明说停止。停止词是「停止协议」（英文 "stop the protocol"），
  说它即全部停用，不再输出任何脚手架，直到用户要求恢复。它不因话题切换或轮次增加而失效。
  若不确定是否仍然适用，答案是适用。这一条防的是行为指令随轮次衰减，那是常驻行为协议的常见失效模式，
  同类协议都显式设了防，本协议正文也已补齐。
- 红线（这样不算收口）：只报完成、没有状态栏；选项是上下文里没有的事；菜单插在执行中途；
  用代码块包裹菜单。另外要认得出自我合理化：「这轮差不多算收口了」「用户就随口一问」「状态栏太长先省了」
  都属于这类，不构成豁免理由。
